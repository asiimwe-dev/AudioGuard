# AudioGuard production dependencies
# Pinned to minor versions for reproducibility; patch updates safe.

# ── Core audio I/O ────────────────────────────────────────────────────────────
numpy>=1.24,<2.0          # vectorised DSP; 2.x ABI not yet stable
scipy>=1.10,<2.0           # STFT, resampling, interpolation
soundfile>=0.12,<1.0       # WAV/FLAC/OGG read/write (libsndfile wrapper)
pydub>=0.25,<1.0           # MP3/M4A/AAC via FFmpeg backend

# ── Error correction ─────────────────────────────────────────────────────────
reedsolo>=1.7,<2.0         # Reed-Solomon ECC

# ── API ───────────────────────────────────────────────────────────────────────
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0
python-multipart>=0.0.9    # UploadFile form parsing

# ── CLI ───────────────────────────────────────────────────────────────────────
click>=8.1,<9.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27                # ASGI test client for FastAPI

# ── Optional: CNN decoder (Phase 3) ─────────────────────────────────────────
# Uncomment when GPU/PyTorch infrastructure is ready.
# torch>=2.2 --index-url https://download.pytorch.org/whl/cpu

# ── Optional: async task queue ────────────────────────────────────────────────
# celery[redis]>=5.3
# redis>=5.0
